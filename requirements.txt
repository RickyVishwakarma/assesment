# Versions are the ones this was built and measured on. Looser pins will
# probably work; these are what the reported numbers came from.
#
#   pip install -r requirements.txt
#   python -m spacy download en_core_web_sm
#
# torch is deliberately NOT pinned here. Install it first, matching your own
# hardware, from https://pytorch.org -- the CUDA build is machine-specific:
#
#   GPU (CUDA 12.8):  pip install torch --index-url https://download.pytorch.org/whl/cu128
#   CPU only:         pip install torch
#
# Everything except the small model runs fine on CPU. The rules approach needs
# none of the deep-learning stack at all.

# --- core: schema, OCR, document generation -------------------------------
pydantic==2.13.4
PyMuPDF==1.27.2.2
pdfplumber==0.11.10
rapidocr-onnxruntime==1.4.4
reportlab==5.0.0
pillow==12.3.0
numpy==2.4.4
python-dateutil==2.9.0.post0

# --- rules / NLP approach -------------------------------------------------
scikit-learn==1.8.0
spacy==3.8.16
rapidfuzz==3.14.6

# --- small model (also needs torch, see note above) -----------------------
transformers==5.5.4
accelerate==1.13.0
datasets==5.0.1
seqeval==1.2.2

# --- local LLM approach ---------------------------------------------------
# Also needs the Ollama runtime and a pulled model:
#   winget install --id Ollama.Ollama     (or https://ollama.com/download)
#   ollama pull qwen2.5:3b-instruct
requests==2.33.1

# --- HTTP API -------------------------------------------------------------
fastapi==0.135.3
uvicorn==0.44.0
python-multipart==0.0.26

# --- tests ----------------------------------------------------------------
pytest==9.1.1
